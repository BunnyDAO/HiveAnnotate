{
  "name": "@hiveannotate/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "hiveannotate-mcp": "./src/bin.ts" },
  "main": "./src/server.ts",
  "exports": { ".": "./src/server.ts" },
  "comment": "Depends on @hiveannotate/core only. Runs as a bare Node stdio process with Electron absent — that independence is the feature.",
  "dependencies": {
    "@hiveannotate/core": "*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  }
}
